const API_URL = "http://192.168.1.18:8000";

const LS_USER_DATA = "userData";
const TOKEN_EXPIRATION = new Date(new Date().getTime() + 1000 * 60 * 60);
const APP_DEBUG = true;

export { API_URL, LS_USER_DATA, TOKEN_EXPIRATION, APP_DEBUG };
